#include<stdio .h> 
void main()
{
int year;
printF("Enter a year\n");
scanf("%d",&year);
if(year % 4==0)
{
if(year%4==0)
{
printf("\n it is leap year.");
else
printf("\n it is not leap year.");
}
}
}
return o;
}
